import _objectSpread from "@babel/runtime/helpers/objectSpread";

/* eslint-disable react/no-unused-prop-types */
import React from 'react';
import { gridSize, math } from '@atlaskit/theme';
import { getLoadingStyle } from './utils';

var getMargin = function getMargin(props) {
  if (props.spacing === 'none') return 0;
  if (props.isOnlyChild) return "0 -".concat(math.divide(gridSize, 4)(props), "px");
  return "0 ".concat(math.divide(gridSize, 2)(props), "px");
};

var IconWrapper = function IconWrapper(props) {
  var style = _objectSpread({
    alignSelf: 'center',
    display: 'flex',
    flexShrink: 0,
    lineHeight: 0,
    fontSize: 0,
    margin: getMargin(props),
    userSelect: 'none'
  }, getLoadingStyle(props));

  return React.createElement("span", {
    style: style
  }, props.children);
};

export default IconWrapper;